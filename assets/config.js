/* Ajustar só para a URL exata do mesmo endpoint em homologação. Não contém segredo. */
window.ERP_SAAS_CONFIG = Object.freeze({
  apiUrl: 'https://api.erpimpar.com.br/george-reuniao/v09/saas_acesso.php',
  timeoutMs: 20000,
  version: '0.7-rc1'
});
